/*This will not be the most beautiful embedding you have ever seen.
 * This is because this excersize was done on windows. Not on linux
 * where I could have done everything natively.*/

#include <stdlib.h>

int main() {
  char* input;
  system("sbcl --script script.lsp");

  return 0;
}
